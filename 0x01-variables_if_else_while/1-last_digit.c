#include <stdlib.h>
#include <time.h>
/* main - Entry
 * Return: 0;
 */
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					do {
						printf("%d\n", n);
						n++;
					} while (
					return (0);
}

